class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x<0:
            return False
        strx=str(x)
        rev=strx[::-1]
        if strx==rev:
            return True
        else:
            return False       
        
