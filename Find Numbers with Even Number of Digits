class Solution(object):
    def findNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        count=0
        for num in nums:
            str_num=str(num)
            if len(str_num)%2==0:
                count+=1
        return count    
